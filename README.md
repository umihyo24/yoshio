# Element Run

依存関係なしで遊べる、Canvas 製の小さな横スクロール・アクションゲームです。

```sh
python3 -m http.server 8000
```

ブラウザで `http://localhost:8000` を開いてください。A/D で移動、右クリック（または Space）でジャンプ、マウス移動で照準、左クリックで射撃、マウスホイールで弾薬を切り替えます。空中で壁に触れると壁滑りになり、そこからジャンプすると壁と反対方向へ跳べます。
